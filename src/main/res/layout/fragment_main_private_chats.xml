<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/windowBackground">

    <!-- Search bar container (compact) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="6dp"
        android:paddingBottom="4dp"
        android:background="@color/surfaceColor">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="34dp"
            android:layout_weight="1"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:background="@drawable/edit_text_background"
            android:paddingStart="8dp"
            android:paddingEnd="4dp">

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@android:drawable/ic_menu_search"
                android:tint="@color/textSecondary"
                android:contentDescription="@null" />

            <EditText
                android:id="@+id/private_chats_search_edit"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:background="@null"
                android:hint="@string/private_chats_search_hint"
                android:textColor="@color/textPrimary"
                android:textColorHint="@color/textSecondary"
                android:textSize="13sp"
                android:paddingStart="6dp"
                android:paddingEnd="4dp"
                android:singleLine="true"
                android:imeOptions="actionSearch" />

            <ImageButton
                android:id="@+id/private_chats_btn_clear_search"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:background="?android:selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_menu_close_clear_cancel"
                android:tint="@color/textSecondary"
                android:contentDescription="@string/action_clear_search"
                android:visibility="gone" />
        </LinearLayout>
    </LinearLayout>

    <!-- Filter bar container (compact chips) -->
    <LinearLayout
        android:id="@+id/private_chats_filter_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="2dp"
        android:paddingBottom="4dp"
        android:background="@color/surfaceColor">

        <TextView
            android:id="@+id/filter_btn_server"
            android:layout_width="0dp"
            android:layout_height="28dp"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/filter_dialogs_server"
            android:textSize="12sp"
            android:textStyle="bold"
            android:layout_marginEnd="3dp"
            android:background="@drawable/chip_filter_selected"
            android:textColor="#FFFFFF" />

        <TextView
            android:id="@+id/filter_btn_all"
            android:layout_width="0dp"
            android:layout_height="28dp"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/filter_dialogs_all"
            android:textSize="12sp"
            android:layout_marginStart="2dp"
            android:layout_marginEnd="2dp"
            android:background="@drawable/chip_filter_unselected"
            android:textColor="@color/textSecondary" />

        <TextView
            android:id="@+id/filter_btn_online"
            android:layout_width="0dp"
            android:layout_height="28dp"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/filter_dialogs_online"
            android:textSize="12sp"
            android:layout_marginStart="3dp"
            android:background="@drawable/chip_filter_unselected"
            android:textColor="@color/textSecondary" />
    </LinearLayout>

    <!-- Subtitle count info (compact) -->
    <TextView
        android:id="@+id/private_chats_count_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:textSize="11sp"
        android:textColor="@color/textSecondary"
        android:background="@color/surfaceColor"
        android:text="" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/dividerColor" />

    <!-- List & Empty state -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <ListView
            android:id="@+id/private_chats_listview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@color/dividerColor"
            android:dividerHeight="1dp"
            android:clipToPadding="false"
            android:scrollbarStyle="outsideOverlay" />

        <LinearLayout
            android:id="@+id/private_chats_empty_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="24dp"
            android:visibility="gone">

            <ImageView
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_private_messages"
                android:tint="@color/textTertiary"
                android:contentDescription="@null" />

            <TextView
                android:id="@+id/empty_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="@string/private_chats_empty_title"
                android:textColor="@color/textPrimary"
                android:textSize="15sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/empty_desc"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:gravity="center"
                android:text="@string/private_chats_empty_desc"
                android:textColor="@color/textSecondary"
                android:textSize="13sp" />
        </LinearLayout>
    </FrameLayout>
</LinearLayout>
